import logging
logging.basicConfig(
    filename=os.path.join(LOG_FOLDER, "asystent_log.txt"),
    level=logging.INFO,
    format="%(asctime)s - %(levelname)s - %(message)s"
)
logging.info(f"Analiza pliku: {full_path} → {status}")
